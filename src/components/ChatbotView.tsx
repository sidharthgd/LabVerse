import React from 'react';

const ChatbotView: React.FC = () => {
    return (
        <div className="chat-container">
            <h1>Chatbot</h1>
            <p>This is the chatbot interface.</p>
        </div>
    );
};

export default ChatbotView;
